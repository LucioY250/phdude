---
description: Show the phdude command list, global options, and exit codes.
allowed-tools: Bash(phdude:*), Bash(npx:*)
phdude-managed: true
---

Arguments: `$ARGUMENTS`

Run:

```
phdude help --json
```

Report the commands relevant to what the researcher asked for rather than pasting the whole
list back. The full reference is `docs/cli.md`; the operating rules that govern every command
are in `.phdude/skills/phdude-core/SKILL.md`.
